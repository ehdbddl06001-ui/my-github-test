# Drive 전용 산출물 등록부 — repo 에 없는 것들

> **왜 이 파일이 있나.** 2026-08-02 에 실험22 를 설계하다가 인용하려던 수치
> (SVEB 전이 관련)가 **repo 어디에도 없다**는 것이 드러났다. 실행이 전부 Colab
> 이었고 산출물이 Drive 에만 떨어져 커밋되지 않았기 때문이다. 대화 기록이 없으면
> 다시 찾을 수 없는 상태였다.
>
> → **규약**: Colab 산출물은 (a) repo 로 회수하거나 (b) 최소한 **여기에 위치를
> 등록**한다. 등록되지 않은 수치는 **인용하지 않는다.**

---

## 2026-07-17 · SVDB 혼합 학습 실험 (S 클래스 데이터 증강)

### 실제 설계 — **cross-DB 외부검증이 아니다** ⚠️

| arm | 학습 | 테스트 |
|---|---|---|
| 대조군 `mit_only` | MIT-BIH DS1 | MIT-BIH **DS2** |
| 실험군 `mit_svdb` | MIT-BIH DS1 + SVDB 14레코드 | MIT-BIH **DS2** |

양쪽 다 **같은 DB 에서 테스트**했다. S 학습 표본을 422 → 8,396 개로 늘린
**데이터 증강** 실험이다.

- ✅ 올바른 서술: "다른 DB 의 S 비트를 훈련에 섞었더니 원래 DB 성능이 나빠졌다"
- ❌ 틀린 서술: "한 DB 에서 학습해 다른 DB 에서 테스트했더니 나빠졌다"

**따라서 이 실험의 수치를 `외부 낙폭(external drop)` 표에 넣으면 안 된다.**
MI 의 PTB-XL → PTBDB 낙폭(+0.09 ~ +0.26)과 같은 축이 아니다.

> 같은 이유로 **실험1′ 의 Icentia `B3 − B2 = +0.1268`** 도 낙폭이 아니다.
> 그건 "리듬 축의 기여가 다른 DB 에서도 재현된다" 이지 "성능이 얼마나 떨어졌나" 가 아니다.

### 🔴 2026-08-02 정정 — 위 문장에 이어 내가 쓴 것이 틀렸다

처음 이 파일에 **"현재 실측으로 보유한 cross-DB 낙폭은 형태 축(MI) 하나뿐이다"** 라고
썼는데 **사실이 아니다.** `mit-bih/PAPER.md §6.5` 에 **MIT-BIH → INCART zero-shot
교차DB 결과가 이미 커밋돼 있다**(`colab_crossdb.py` · `colab_incart_prep.py`).

| 특징집합 | S_PR | 기저율(1.12%) 대비 | V_PR |
|---|---:|---:|---:|
| 형태만 (WST+MORPHO+REPOL+KOOPMAN+GNN) | 0.0197 | 1.8× | 0.690 |
| **+ RHYTHM(10)** | **0.0882** | **7.9×** | **0.854** |
| + BN 적응(무라벨) | 0.0762 | 6.8× | 0.781 |

그리고 이 결과는 **실험22 의 핵심 가설을 이미 한 번 검정하고 있다**:

1. **`V`(형태 정의 클래스)는 리듬 없이도 전이된다** — V_PR 0.690. 넓은 QRS 는
   DB·장비·인구에 불변이다.
2. **`S`(타이밍 정의 클래스)는 리듬 없이는 우연 수준**(1.8×)이고 리듬을 넣으면 7.9×.
3. **BN 적응이 개선을 못 준다** → 입력 분포 shift 가 아니라 **판별 축의 유무** 문제다.
   (이게 세 번째 항목이라 특히 강하다 — 도메인 이동으로 설명되지 않는다.)

⚠️ 단 §6.5 에는 **미해결 한계 L1~L5** 가 붙어 있고 저자 스스로 **"하한으로 읽어야
한다"** 고 적어놨다(INCART 주석 정합·정규화 비대칭·PID 를 레코드로 셈·AF 혼입 등).

### 그래서 실험22-A 의 일이 바뀐다

"없는 것을 새로 재는" 게 아니라 **"흩어진 것을 비교 가능한 형식으로 모으는"** 것이다.

**가장 큰 걸림돌은 지표다.** 지금 축마다 다른 지표를 쓰고 있다:

| 축 | 실험 | 지표 | 코호트 기저율 |
|---|---|---|---|
| 형태(MI) | 20 / 20c | **AUROC** 낙폭 | PTBDB MI 49% |
| 비트(S·V) | §6.5 | **PR-AUC** + lift | INCART S 1.12% · MIT-BIH DS2 S 3.73% |

PR-AUC 는 기저율에 붙어 있어 **코호트 간 직접 비교가 불가능**하다. lift 로 환산해도
**천장이 코호트마다 다르다**(MIT-BIH DS2 = 1/0.0373 = 26.8× · INCART = 89.3×) —
실험22 에서 위험비를 오즈비로 바꾼 것과 **같은 종류의 문제**다.

→ **AUROC 가 유일하게 안전한 공통 지표다**(순위 기반 · 유병률 무관).
실험22-A 는 저장된 확률에서 **AUROC 를 다시 뽑아** 축 간 낙폭을 같은 자로 재야 한다.

참고로 lift 로만 환산해 보면(비교용, 채점용 아님):

| | lift | 천장 |
|---|---:|---:|
| MIT-BIH DS2 within · 전체 | 22.5× | 26.8× |
| MIT-BIH DS2 within · #232 제외 | 8.6× | 26.8× |
| INCART cross · 형태만 | 1.8× | 89.3× |
| INCART cross · +RHYTHM | 7.9× | 89.3× |

천장이 3배 넘게 다르므로 **이 표로 낙폭을 말하면 안 된다.**

### Drive 위치

| 종류 | 폴더 ID | 파일 |
|---|---|---|
| 노트북 | `1vAsEMQEf-JcGXyb1YH2tkjRCdfsi0vCU` | `stage1_svdb.ipynb` (07-17 08:49 UTC) · `stage1b_proba (1).ipynb` (10:22 UTC) |
| 확률 pkl | `1ZSiLz1xoT-8aHPjE3yc4TBHxOYor2vD2` | `mit_only_1000~1004.pkl` (각 430,758 B) · `mit_svdb_1000~1004.pkl` (각 430,760 B) · 합 ~4.3 MB |
| 데이터 캐시 | `1Jy3gf75llhwF2tebJcegYcEuiL2kTIrn` | `data_mit.npz` 33.2 MB · `data_mitsvdb.npz` 67.6 MB |

각 pkl 에 `proba`(float16 원값)와 `train_prior` 가 들어 있다 →
**재학습 0회로** PR-AUC · ROC · 임계값 분석 · prior 보정을 다시 뽑을 수 있다.

⚠️ `stage1.jsonl` (143 KB, 07-23 생성)도 같은 공간에 있으나 **날짜가 달라 이 실험
산출물인지 미확인**이다. 쓰기 전에 내용을 확인할 것.

### 🔴 2026-08-02 실측 — pkl 은 발견됐지만 **라벨이 없다**

실험22-A CELL 3 이 pkl **10/10 개**를 찾았다. 경로:
`/content/drive/MyDrive/ecg_out/runs_s1p/mit_only_1000.pkl` (외 9개)

```
['arm','seed','proba','train_prior','pred','f1','macro4','nsv',
 'n_train','n_S_train','n_pat_train','mins']
```

**`y` 가 없다.** 그리고 `len(proba) = 47,801` 인데 현재 파이프라인의
MIT-BIH DS2 는 **49,295** 비트다 — **1,494 비트 차이**.

| 뜻 | 결과 |
|---|---|
| 07-17 실행의 테스트셋은 지금의 DS2 와 **같은 배열이 아니다** | 라벨을 빌려오면 **조용히 다른 실험**이 된다 |
| 그 실행 자체는 두 arm 이 **같은 테스트셋**을 봤다 | arm 간 **짝지은 비교는 여전히 유효**하다 |

→ CELL 3 을 다시 짰다(2026-08-02). 하는 일:
① 저장된 `f1`·`macro4`·`nsv`·`n_S_train` 을 **라벨 없이 회수**해 짝지은 Δ 를 낸다
② `data_mit.npz`(그 실행 **자신의** 캐시)에서 라벨을 찾아, DS2 마스크 길이가
   `len(proba)` 와 **정확히 같을 때만** AUROC 를 재계산한다
③ 어느 쪽이든 **레코드별 비트 수를 비교**해 1,494 의 출처(레코드 통째 누락인지
   전 구간 소실인지)를 진단문으로 남긴다

픽스처: `pipelines/test_exp22a_g1.py` — 노트북 셀을 **직접 읽어** 가짜 Drive 위에서
다섯 경로를 실행한다. 규약은 `SCORING_RULES.md` **R10 · R10-b · R10-c**.

⚠️ `f1`·`macro4` 는 동작점에 붙은 지표라 아래 인용 잠금을 **해제하지 못한다.**

### ✅ 2026-08-02 확정 — 07-17 실행의 정체 (`forensics_0717_split.py` 실측)

**결론: 프로토콜은 표준이고, 데이터에는 국소적 결함이 있다. 실험은 쓸 수 있다.**

캐시 `data_mit.npz` 는 3분할이고 그룹 배열 `gp`·`gv`·`gt` 가 `<U9` **문자열**
(`'mitdb:100'`)이라, 정수만 받는 코드는 "레코드 ID 없음" 으로 오판한다.

| 분할 | n | 레코드 | N | S | V | F |
|---|---:|---:|---:|---:|---:|---:|
| train | 38,283 | 17 (DS1) | 34,563 | 422 | 2,891 | 407 |
| val | 11,312 | 5 (DS1) | 10,153 | 450 | 708 | 1 |
| test | 47,801 | 22 (DS2) | 42,542 | 1,808 | 3,074 | 377 |

- `train ∪ val` = **DS1 정확히 일치** · `test` = **DS2 정확히 일치** · 환자 누수 **0**
- val = `[101, 106, 112, 201, 209]`. **#209 가 S 383개**를 갖고 val 로 갔다
  → 그래서 train S 가 944 가 아니라 **422** 다(결함이 아니라 분할 결과)
- 07-17 은 **4클래스**(N/S/V/F), 이 실험은 3클래스 → 겉보기 차이 1,494 는
  `N/S/V 부족 1,871 − F 초과 377` 의 **상쇄값**이다

#### 손실 기전은 하나가 아니다 — **세 개**다

전체 부족 3,260비트(3.26%). 그런데 **균일하지 않다**(→ R10-d):

| # | 기전 | 증거 | 규모 |
|---|---|---|---:|
| 1 | **레코드 단위 사고** | #228 2,053→398(80.6% 소실) · #114 1,875→1,013(46.0%) | 2,517 (**77%**) |
| 2 | **심볼→AAMI 매핑 누락** | N 무손실인데 S·V 만 소실(→ R10-e) | ~200 |
| 3 | 경계 창 잘림 | 여러 레코드 N 0.05~0.6% | 나머지 |

손실 **0** 인 레코드가 13/44 이고, 5% 넘는 건 3개(#114·#203·#228)뿐이다.

전달받은 **"전 레코드 균일 3~4% 검출기 손실"** 은 **성립하지 않는다.** 근거로 든
"손실률 S 1.58% < N 3.84% < V 4.53% — 생리학적으로 정확한 순서" 는 **풀링
아티팩트**다: #228·#114 가 N 을 2,446개 끌고 가 N 비율을 밀어올렸고, S 는
#232(1,381개, 손실 **1**)가 분모를 채워 눌렀다. 레코드별로 보면 순서가 다르다.

**`PEAK_SOURCE='detected'` 가설은 기각한다** — R파 검출기는 class-blind 이므로
#201 에서 N 을 1,635개 중 1개만 놓치면서 S 를 128개 중 66개 놓칠 수 없다.

#### 그래서 07-17 실험은 **사용 가능**하다

- 두 arm 이 **같은 테스트셋**(47,801)을 봤다 → 짝지은 비교 유효
- 표준 DS1/DS2 · 환자 누수 0
- S 평가를 지배하는 **#232 는 1,381/1,382 로 온전**(손실 1개)
- 결함은 주로 N 이고 #228 한 레코드에 몰려 있다

⚠️ 다만 **학습 표본이 DS1 전체가 아니다**(17/22). 이 실험(22 전체 학습)의 within
기준선과 **같은 행에 놓지 않는다** — 별도 코호트로 각주 처리한다.

### 외부에서 전달된 '2026-07-22 kinkmap 원장' — **이 repo 에 없다** 🔴

1,494 의 원인으로 아래 설명이 전달됐다. **채택한 것과 격리한 것을 구분해 적는다.**

**채택 — 우리 데이터로 검증되는 부분** ✅

| 주장 | 검증 |
|---|---|
| 겉보기 1,494 는 **부족(N/S/V) + 초과(F) 의 상쇄값**이다 | pkl 키 `macro4`·`nsv` 가 4클래스를 증언한다. CELL 3 이 `nsv` 합 vs `len(proba)` 로 F 를 실측한다 |
| '레코드 통째 누락' 은 **클래스 프로파일이 죽인다** | #232 가 DS2 S 의 75.2% 라는 사실은 `PAPER.md` 관찰3 에 있다. S 손실이 작으면 레코드 소실일 수 없다 |
| 길이 일치는 정렬의 증거가 아니다 | → **R10-b** 로 규약화. 픽스처 시나리오 D 가 검정 |

→ 그래서 CELL 3 에 세 가지를 추가했다: `proba` **열 수** 출력 · `nsv` 합 대조 ·
**클래스별 개수까지 맞아야** 정렬 인정. 이건 원래 빠져 있던 검사였다.

**2026-08-02 실측 후 갱신**: 아래 표의 **분할 수치는 캐시에서 확인**됐다(위 절 참조).
따라서 인용 가능하다 — R9 의 '격리 → 실측 → 해제' 가 정상 작동한 사례다.
다만 **원인 설명(검출기 손실)은 실측으로 기각**됐다. 수치는 맞고 해석이 틀렸다.

**여전히 격리 — 등록되지 않은 수치** 🔴 (R9)

`kinkmap` · `PEAK_SOURCE` · `week01_ecg_v8.ipynb` · `42542` 를 **repo 전체에서
찾을 수 없다.** 07-17 pkl 을 격리한 것과 **같은 이유로** 아래 표도 인용하지 않는다:

> v8 N 42,542 / S 1,808 / V 3,074 / F 377 · 현재 N 44,239 / S 1,837 / V 3,220

두 가지가 더 걸렸고, **실측으로 판정됐다**:

1. **같은 양에 두 값** — 본문 손실 `N 1,717 / V 147`, 표 `N 1,697 / V 146`.
   → 실측 DS2 쪽 손실은 **N 1,696 · S 29 · V 146**. 표 쪽이 맞았다(N 은 1 차이).
2. **총계가 1 다름** — 표는 49,296, 우리 실측 DS2 는 **49,295**
   (N/S/V = 44,238/1,837/3,220). **우리 쪽이 맞다.**

repo 의 week01 노트북 둘(`week01_ecg_1dcnn.ipynb`·`ailab_week01_ecg_mitbih.ipynb`)은
**둘 다 `ann.sample`**(주석 R)을 쓴다. 검출기를 쓴 v8 은 여기 없다.

→ **2026-08-02: `PEAK_SOURCE='detected'` 가설은 실측으로 기각됐다**(위 절 · R10-e).
손실이 class-blind 가 아니었다. 다만 v8 파일 자체는 여전히 repo 에 없다.

**프레이밍은 반대로 본다**(기전 자체가 기각됐으므로 무효지만 교훈은 남긴다). "07-17 이 흘리고 있었고 나중에 **고쳤다**" 는 표현은
쓰지 않는다. v8 이 검출기 R 을 **의도적으로**(배포 조건 재현) 썼다면, 주석 R 로 옮긴
것은 고친 게 아니라 **더 관대한 조건으로 이동한 것**이다 — 주석 R 은 실제 웨어러블에
없는 정보다. 실험17·18 이 웨어러블 배포 조건을 다루는 만큼 **외적 타당도 항목**이다.

### 인용 대기 표의 두 줄은 **서로 다른 검정**이다

`V: Δ=+0.0542, p=0.031` — 2⁻⁵ = 0.03125. 이건 **5/5 부호검정**이다(t검정이 아니다).
`S: Δ=−0.219, t=−4.36` — 이건 t검정이다.

시드가 5개인 이상 부호검정으로는 0.031 보다 작은 p 가 **나올 수 없다**. 한 표 안에
검정이 섞여 있으므로, 카드에 옮길 때 각 줄의 검정을 명시한다.

### ✅ 2026-08-02 【G1】 해금 — AUROC 를 실측했다

라벨 복원 성공(길이 47,801 **와** 학습쪽 지문 (38283, 17, 422) 이중 일치).

| 클래스 | 지표 | mit_only | mit_svdb | Δ | 95% CI | |
|---|---|---:|---:|---:|---|---|
| **S** | F1@argmax | 0.5061 | 0.3831 | **−0.1229** | [−0.2144, −0.0315] | 0 제외 |
| **S** | **AUROC** | 0.9223 | 0.9020 | **−0.0203** | [−0.0543, +0.0137] | **0 포함** |
| **V** | F1@argmax | 0.7332 | 0.8133 | **+0.0800** | [+0.0436, +0.1164] | 0 제외 |
| **V** | **AUROC** | 0.9506 | 0.9767 | **+0.0261** | [+0.0076, +0.0445] | 0 제외 |

**V 는 두 지표가 일치하고, S 는 갈린다.** S 는 F1 이 24.3% 상대 하락인데 AUROC 는
2.2% 이고 CI 가 0 을 포함한다.

이건 **사전확률 shift** 로 설명된다:

| | 학습 S 비율 | 테스트 실제(0.03782) 대비 |
|---|---:|---|
| mit_only | 0.01102 | 3.4배 **적게** 배움 |
| mit_svdb | 0.11226 | 3.0배 **많게** 배움 |

→ **10.2배** 차이. 동작점이 안 움직였을 리가 없고, F1 은 동작점에 붙은 지표다.

→ CELL 3b(`【G1-b】`)를 추가했다. 순위 지표 3종(AUROC · PR-AUC · R-precision)과
임계값 지표(F1)를 나란히 놓고 **효과크기 비** `REL = max|Δ순위| / |ΔF1|` 로
'임계값 이동 vs 판별력 변화' 를 가른다. 픽스처 `pipelines/test_exp22a_g1b.py` 가
순위보존 이동(REL 0.1%)과 진짜 훼손(REL 239%)을 심어 검정한다.

⚠️ **판정을 유의성으로 하면 안 된다** — 픽스처에서 순위보존 이동인데 PR-AUC Δ
−0.0003 의 CI 가 0 을 제외했다(시드 5개가 거의 결정론적). R5 계열 함정.

### ✅ 2026-08-02 【G1-b】 — 임계값이 아니라 **판별력**이다 (내 가설이 틀렸다)

사전확률이 10.2배 다르니 임계값 이동일 것이라 예상했는데 **아니었다.**

| 클래스 | 지표 | 성격 | mit_only | mit_svdb | Δ | 95% CI |
|---|---|---|---:|---:|---:|---|
| S | AUROC | 순위·전체 | 0.9223 | 0.9020 | −0.0203 | [−0.0543, +0.0137] 미결 |
| **S** | **PR-AUC** | 순위·**상위가중** | 0.5138 | 0.2944 | **−0.2194** | [−0.3591, −0.0797] |
| **S** | **R-prec** | 순위·상위k만 | 0.5381 | 0.3287 | **−0.2094** | [−0.3096, −0.1092] |
| S | F1@argmax | 임계값 | 0.5061 | 0.3831 | −0.1229 | [−0.2144, −0.0315] |
| V | AUROC | 순위·전체 | 0.9506 | 0.9767 | +0.0261 | [+0.0076, +0.0445] |
| V | PR-AUC | 순위·상위가중 | 0.7982 | 0.8960 | +0.0978 | [+0.0445, +0.1511] |

효과크기 비 **REL = 178.5%**(S) · **122.2%**(V) → 둘 다 **판별력 변화**.
임계값 재보정으로 복구되지 않는다.

**AUROC 는 왜 미결인가**: 기저율 3.78% 라 AUROC 는 압도적 다수인 N 과의 분리에
지배된다. 손상은 **목록 맨 위**에서 일어났고 AUROC 는 거기에 둔감하다.
→ 상위 1,808개 안의 진짜 S 가 **973 → 594 (−379개)**.

**이게 '음의 전이(negative transfer)' 다.** 타 DB 의 S 를 섞자 모델이 MIT-BIH 의
어떤 비트들을 **자신 있게 S 라고 잘못 부르게** 됐다. 데이터를 늘리면 좋아진다는
가정이 맥락정의 클래스에서 깨진 사례다.

⚠️ 그리고 F1(−0.1229)이 PR-AUC(−0.2194)보다 **작다**. 사전확률 상승이 재현율을
올려 **손상을 일부 가렸다**. 임계값 지표만 봤으면 피해를 과소평가했을 것이다.

### 🔓 인용 대기 표 — S 행은 **정확히 재현되어 해금**

| | 전달값 | 실측 | |
|---|---|---|---|
| **S** | Δ = −0.219 · **t = −4.36** | **PR-AUC** Δ = −0.2194 · **t = −4.360** | ✅ **해금** |
| V | Δ = +0.0542 · p = 0.031 | AUROC +0.0261 · PR-AUC +0.0978 · R-prec +0.0588 · F1 +0.0800 | 🔴 **여전히 격리** |

S 는 소수 셋째 자리까지 일치한다 → **그 값의 지표는 PR-AUC 였다**(그동안 불명이었다).
V 의 `+0.0542` 는 네 지표 어느 것과도 안 맞는다. `p = 0.031` = 2⁻⁵ 이므로
**5/5 부호검정**이고, 부호 주장(V 5/5 개선)은 실측과 일치하지만 **크기는 미확인**이다.

### 🔄 2026-08-02 【G1-c】 — S 결론이 **또 뒤집혔다**

`#232` 를 쪼개 보니 G1-b 의 "S 판별력이 손상됐다" 가 부정확했다.

| 코호트 | S 기저율 | AUROC Δ | PR-AUC Δ | R-prec Δ |
|---|---:|---:|---:|---:|
| 전체 (1,808) | 3.78% | −0.0203 미결 | **−0.2194** | **−0.2094** |
| **#232 만** (1,381) | 77.63% | **+0.0204** | **+0.0053** | **+0.0130** |
| #232 제외 (427) | 0.93% | +0.0118 미결 | −0.0746 미결 | **−0.1171** |

**#232 안에서는 mit_svdb 가 오히려 낫다.** S 의 76% 를 가진 환자에서 안 나빠졌는데
전역은 −0.2194 다. 부분과 전체가 반대인 **구성 효과**다.

상위 1,808 목록의 구성 변화가 기전을 보여준다:

| 슬롯이 늘어난 레코드 | 얻은 슬롯 | 그 레코드의 **진짜 S** | 최소 위양성 |
|---|---:|---:|---:|
| #210 | +272 | **9** | 263 |
| #219 | +182 | **7** | 175 |
| #202 | +110 | 48 | 62 |
| #231 | +46 | **1** | 45 |
| #221 | +43 | **0** | 43 |
| **합** | **+653** | **65** | **≥588** |

밀려난 쪽: #232 −328 · #222 −169 · #200 −91 · #233 −83.
진짜 S 가 상위에서 **379개** 빠지고 그 자리를 **N 이 378개** 채웠다.

→ **가설: 환자 안 순위는 멀쩡한데 환자 간 점수 눈금이 깨졌다.** SVDB 를 섞자
모델이 #210·#219 같은 특정 기록에 전반적으로 높은 S 점수를 주고, 그게 다른 기록의
진짜 S 를 전역 목록에서 밀어낸다.

⚠️ **#210·#219 가 왜 뜨는지는 아직 가설이다.** SVDB 는 상심실성 부정맥 DB 이므로
심방세동·조동 구간이 있는 기록에서 위양성이 나는 그림이 그럴듯하지만, MIT-BIH 의
리듬 주석(`aux_note`)을 읽기 전까지 **인용하지 않는다**(R9).

→ CELL 3d(`【G1-d】`)가 이 가설을 검정한다: 점수를 **레코드 안에서 순위정규화**하면
환자 안 순위는 100% 보존되고 환자 간 눈금만 사라진다. 그 뒤 전역 지표가 돌아오면
눈금 문제다. **주 판정은 레코드 매크로 R-prec**(각 레코드 안에서만 채점 → 환자 간
비교가 정의상 0)로 한다. 픽스처 `pipelines/test_exp22a_g1d.py`.

⚠️ 이건 배포에서 실제로 가능한 처치다 — 홀터 분석은 어느 기록을 보는지 알기 때문에
레코드 내 정규화는 라벨 없이 쓸 수 있다(우리 파이프라인의 `_medref`·환자상대 리듬
특징과 같은 발상). 즉 "고칠 수 있는 실패" 일 가능성이 있다.

### ✅✅ 2026-08-02 【G1-d】 결론 — **SVDB 증강은 S 를 개선했다** (원 결론 전면 뒤집힘)

| 처치 | 지표 | mit_only | mit_svdb | Δ | 95% CI |
|---|---|---:|---:|---:|---|
| 원본(전역) | PR-AUC | 0.5138 | 0.2944 | **−0.2194** | [−0.3591, −0.0797] |
| **레코드내 순위정규화** | PR-AUC | 0.0851 | 0.0951 | **+0.0100** | [+0.0037, +0.0164] |
| **레코드 매크로**(S≥20, 7레코드) | R-prec | 0.5498 | 0.6129 | **+0.0631** | [+0.0120, +0.1143] |

**회복비율 95.4%.** 전역 하락의 95% 가 **환자 간 점수 눈금** 탓이고, 환자 **안에서는
오히려 좋아졌다**. 채점 7레코드가 S 의 98.1% 를 덮는다.

→ **"타 DB 의 S 를 섞으면 S 가 망가진다" 는 결론은 성립하지 않는다.**
망가진 것은 **기록 간 점수 비교 가능성**이고, 그건 배포에서 고칠 수 있는 종류다.

⚠️ **다만 순위정규화를 처방으로 쓰면 안 된다.** 정규화하면 두 arm 모두 절대 성능이
폭락한다(mit_only PR-AUC 0.5138 → 0.0851). '이 기록은 S 가 많다' 는 **진짜 정보까지**
버리기 때문이다. 정규화는 **진단 도구**이고, 처방은 기록 수준 사전확률을
무라벨로 추정해 **보정**하는 쪽이어야 한다.

⚠️ CI 는 **시드 5개**에 대한 것이고 매크로는 **레코드 7개** 평균이다 — 레코드 표집
변동이 안 잡혔다(R8). CELL 3d 에 레코드 부트스트랩을 추가했다. 넓은 쪽으로 판정한다.

### ⚠️ 남은 최대 한계 — S 양성의 76% 가 **#232 한 명**

테스트 S 1,808개 중 **1,381개가 #232**다. 위 S 결론이 코호트 성질인지 환자 한 명의
성질인지 아직 못 가른다. → CELL 3c(`【G1-c】`)가 #232 를 빼고 다시 재고, 상위 목록을
**무엇이 채웠는지**(클래스별·레코드별) 센다. 픽스처 `pipelines/test_exp22a_g1c.py` 가
'#232 에만 손상' vs '전 레코드 손상' 두 시나리오로 판별력을 검정한다.

### 인용 대기 중인 수치 — **아직 검증 안 됨** 🔴

아래는 대화로 전달된 값이며 **repo 에서 재계산된 적이 없다.**
`실험22-A` 에서 pkl 로 재계산해 파일로 고정하기 전까지 **논문·카드에 인용 금지**.

| 클래스 | 방향 | 전달받은 값 |
|---|---|---|
| `V` (형태 정의) | 5/5 개선 | Δ = +0.0542, p = 0.031 |
| `S` (맥락 정의) | 0/5 악화 | Δ = −0.219, t = −4.36 |

**2026-08-02 대조 결과**: 실측 AUROC 는 V **+0.0261**, S **−0.0203**. 전달값과
**부호는 같고 크기가 크게 다르다**(S 는 10배). 지표가 뭐였는지 불명이므로 위 표는
**여전히 인용 금지**. 특히 `p = 0.031` 은 2⁻⁵ = 0.03125 로 **5/5 부호검정**이고
`t = −4.36` 은 t검정이다 — 한 표에 두 검정이 섞여 있다.
그리고 S 의 '악화' 는 실측상 **임계값 이동**일 가능성이 크다(위 G1 절).

`V` 가 **양성 대조군** 역할을 한다는 점이 이 결과의 힘이다 — "실험이 망가진 게
아니라 `S` 만 다르게 행동한다" 를 같은 실행 안에서 보인다. 오라클 prior 보정으로도
복구되지 않았다면 **임계값 문제가 아니라 표현 문제**라는 뜻이 된다.

→ 명명: **"축별 상반 반응"**. **"낙폭 비교" 가 아니다.**

### 실험22-A 에서 할 일

1. pkl 10개 · 노트북 2개를 `mit-bih/` 로 회수하고 **수치를 파일로 고정**한다.
2. 위 표를 pkl 에서 **재계산**해 P-D1 근거로 삼는다(현재는 미검증 인용).
3. **진짜 cross-DB 낙폭을 잰다**(학습 1회):
   - `SVDB 학습 → MIT-BIH DS2` = cross
   - `SVDB 학습 → SVDB 홀드아웃` = within (기준선)
   - 낙폭 = within − cross → MI 낙폭과 **같은 형식**이라 비교표에 그대로 들어간다
4. ⚠️ **SVDB 홀드아웃은 반드시 레코드(환자) 단위 분할.** 아니면 within 기준선이
   부풀려져 낙폭이 **과대추정**된다. → `data_mitsvdb.npz` 에 **레코드 ID 배열이
   있는지 먼저 확인**하고, 없으면 여기서 중단한다(만들어내지 않는다).


---

## 2026-08-02 · 실험22-A 자산 (업로드 완료)

| 파일 | Drive 위치 | 크기 | 상태 |
|---|---|---:|---|
| `colab_crossdb.py` | `MyDrive/mitbih/` (`1dZO4EB…`) | 11,573 B | 기존 · **repo 사본과 바이트 일치 확인** |
| **`colab_crossdb_split.py`** | `MyDrive/mitbih/` (`1j6iHxV…`) | **7,735 B** | **신규 업로드** · repo 와 바이트 일치 |
| **`colab_step12_wst.py`** | `MyDrive/mitbih/` (`1iq_qqB…`) | **4,522 B** | **교체 업로드**(md5 `4a00dc53938f0aa4b64bf5dd1826b7a7`) · repo 와 바이트 일치 |
| `colab_step12_wst_OLD.py` | `MyDrive/mitbih/` (`11HvkGV…`) | 3,772 B | 구버전 보존용 · **`exec` 하지 말 것** |

폴더: `mitbih` = `1AVfqzGajzPN46dUWkiiLtBNPLpatyurW`

### `colab_step12_wst.py` 를 왜 교체했나 (2026-08-02)

실험22-A CELL 4 가 `ModuleNotFoundError: No module named 'kymatio'` 로 멈췄다.
이 파일은 kymatio 가 없으면 **자동으로 pip install 을 하는데**, 그 다음
`importlib.invalidate_caches()` 를 부르지 않았다. 파이썬은 **한 번 실패한 import 의
음성 결과를 캐시**하므로, 설치에 성공해도 바로 뒤의 `from kymatio.torch import
Scattering1D` 가 같은 세션에서 계속 실패한다. `colab_bootstrap.py::_ensure` 는 원래
이 한 줄을 부르는데 여기만 빠져 있었다.

Drive MCP 에는 **삭제·갱신 도구가 없고 `create_file` 은 중복을 만든다.** 그래서
사용자가 구파일을 `colab_step12_wst_OLD.py` 로 **먼저 rename** 한 뒤 같은 이름으로
새로 올렸다. → `mitbih` 폴더에 `colab_step12_wst.py` 는 **정확히 하나**다(확인함).
노트북은 CELL 1 에서 `!pip -q install kymatio` 도 미리 돌리므로 **이중 방어**다.

**원본을 덮어쓰지 않고 파일을 나눈 이유**: Drive 사본을 덮어쓰면 되돌릴 수 없고,
같은 이름으로 새로 올리면 **중복 파일**이 생겨 `exec` 가 어느 쪽을 읽을지 불확실해진다.
`colab_crossdb_split.py` 는 `colab_crossdb.py` 의 module-level 심볼에 의존하므로
**반드시 그 다음에** `exec` 한다(노트북 CELL 4 가 순서와 심볼 존재를 검사한다).

---

## 2026-08-03 · 퀘스트46 Q7·Q8 실행 산출물 (R9 등록)

두 실행 모두 **학습 0회**다. Colab 이 남긴 위치를 등록해 둔다 — 등록 안 된 수치는
인용하지 않는다(R9).

| 실행 | Drive 위치 | 로그 카드 | 노트북 |
|---|---|---|---|
| Q7 `svdb-cohort-check` | `MyDrive/MedKOS/ecg-model/runs/20260803T0102_quest46_q7_svdb/` | `ailab-2026-0049` | `notebooks/quest46_q7_svdb_cohort.ipynb` |
| Q8 `weighted-macro` | `MyDrive/MedKOS/ecg-model/runs/20260803T0103_quest46_q8_wmacro/` | `ailab-2026-0050` | `notebooks/quest46_q8_weighted_macro.ipynb` |

각 폴더에 `config.json`(관문 판정 + arm 별 값 + 레코드별 값 전량)이 들어 있다.

### 재사용 가능한 캐시

| 파일 | 위치 | 내용 |
|---|---|---|
| **`svdb_ann_counts.json`** | `MyDrive/MedKOS/ecg-model/data/` | SVDB 78레코드의 AAMI N/S/V 주석 카운트. Q7 노트북이 있으면 재다운로드를 건너뛴다 |
| `exp22a_probs_s5_v1.npz` | `MyDrive/MedKOS/ecg-model/data/` | 실험22-A 예측 캐시. Q1·Q8 이 이것만 읽고 학습 0회로 돌았다 |

⚠️ `svdb_ann_counts.json` 은 **주석 카운트일 뿐 신호가 아니다.** SVDB 신호(`.dat`)는
아직 안 받았다 — Q7-B 에서 필요하다.

⚠️ SVDB 는 **128Hz**, MIT-BIH 는 360Hz 다(`svdb_prep.py` F4). Q7-B 에서 zero-shot
성능이 떨어지면 도메인 차이인지 샘플링레이트 차이인지 **리샘플링 대조군으로 분리**해야
한다. 이 구분 없이 낙폭을 인용하지 않는다.

---

## 2026-08-03 · Q7-B 하니스 업로드 (해소됨) — 자리표시자 사고 기록

| 파일 | Drive 위치 | 크기 | 상태 |
|---|---|---:|---|
| **`colab_crossdb_svdb.py`** | `mitbih/` (`1FpUPfr…`) | **14,645 B** | ✅ **정상** · repo 와 바이트 일치 · 같은 이름 **1개**만 존재(확인함) |
| ~~`colab_crossdb_svdb.py`~~ | ~~`mitbih/` (`1k7AzIp…`)~~ | ~~17 B~~ | 🗑️ 사용자가 삭제함 |

**무슨 일이 있었나**: 내가 `create_file` 에 실제 내용 대신 `＿PLACEHOLDER＿` 를 넣어
올렸다. Drive MCP 에는 **삭제·갱신 도구가 없어** 내가 고칠 수 없었고, 같은 이름으로
다시 올리면 **중복 파일**이 생겨 `exec` 가 어느 쪽을 읽을지 불확실해진다
(`colab_step12_wst.py` 때와 같은 함정). 사용자가 자리표시자를 지운 뒤 다시 올려 해소.

**교훈**: Drive 는 되돌릴 수 없다. `create_file` 호출 전에 **올릴 내용을 실제로 읽어
넣었는지** 확인하고, 올린 뒤 **크기·중복 여부를 조회로 확인**한다.

**재발 방지**: Q7-B 노트북 CELL 2 가 이제 **파일 존재만으로 통과시키지 않는다** —
`colab_crossdb.py`→`def run_crossdb` · `colab_crossdb_svdb.py`→`def run_crossdb_svdb` ·
`svdb_labels.py`→`def build_labeled` 심볼이 내용에 있는지까지 보고, 없으면 `AssetError`
로 즉시 멈춘다. 픽스처 `test_quest46_q7b.py` ⑫가 이 방어를 세 파일 모두에서 검정한다.

### Q7-B 가 쓰는/만드는 파일

| 파일 | 위치 | 비고 |
|---|---|---|
| `svdb_data5.npz` | `mitbih/` | **아직 없다.** CELL 3 이 `build_labeled()` 로 만든다(신호 다운로드) |
| `svdb_feats_q7b.npz` | `mitbih/` | Q7-B 전용 특징 캐시 |
| `q7b_svdb_probs_s5.npz` | `MedKOS/ecg-model/data/` | 예측 + 라벨 + 레코드 id (R10) |

⚠️ **`svdb_data.npz`(447MB, `1_wfDKM…`, 07-27 빌드)를 쓰면 안 된다.** `svdb_prep` 은
`rr = np.diff(ann.sample)` 로 RR 을 만들어 `'+'`(리듬변경) 주석 자리에서 **가짜 RR** 이
생긴다. S 는 RR 로 정의되는 클래스라 치명적이다. 같은 이유로 Drive 의 `svdb_feats/`
**폴더**(07-27 산출물)도 재사용하지 않는다 — 그 오염 빌드 기준이라 정렬이 다르다.

### `colab_crossdb_svdb.py` — R16 수정본으로 교체 완료 (2026-08-03)

| 세대 | Drive id | 크기 | 상태 |
|---|---|---:|---|
| ① 자리표시자 | `1k7AzIp…` | 17 B | 🗑️ 삭제됨 |
| ② 초판 | `1FpUPfr…` | 14,645 B | 🗑️ 삭제됨 — `_svdb_load` 에 연속번호 fallback 이 있었다 |
| ③ **현행** | **`19TOaCs…`** | **15,949 B** | ✅ repo 와 바이트 일치 · 같은 이름 **1개**만 존재(확인함) |

②가 이번 ID 매핑 사고의 원인이다(→ `SCORING_RULES.md` **R16**). ③에서 고친 것:

- `wfdb.get_record_list("svdb")` 실패 시 **연속 목록으로 대체하지 않는다** — 예외를 던진다
- 만든 레코드 번호가 원본 목록의 **부분집합인지 자가검증** (`set(num) - set(recs)`)
- `wfdb` 미설치면 **설치는 해준다**(값 날조와 다르다) + `importlib.invalidate_caches()`

**교체 절차 메모**: Drive MCP 에는 삭제·갱신 도구가 없다. 사용자가 구파일을 **먼저
삭제**한 뒤 같은 이름으로 새로 올린다. 같은 이름이 둘이면 `exec` 가 어느 쪽을 읽을지
불확실해지므로, 업로드 후 반드시 **크기·중복을 조회로 확인**한다.

### Q7-E 실행 산출물 (2026-08-03) — 전수 감사에 쓴 원본

| 파일 | Drive id | 크기 | 용도 |
|---|---|---:|---|
| `runs/20260803T0551_quest46_q7e_anchor_sweep/` | `1XRLYdd…` | 폴더 | Q7-E 실행 디렉터리 |
| └ `config.json` | **`1lHyYwH…`** | 68,741 B | **개체별 70건 전수** — `pipelines/audit_anchor.py` 입력 |
| └ `log.txt` | `1-CQmFU…` | 8,232 B | 실행 로그 |
| └ `result.json` | `17Bo2bg…` | 1,174 B | 관문 판정 + 매크로 |
| └ `figures/q7e_anchor_sweep.png` | `14DI9Rr…` | 163,426 B | 3패널 그림 |

`config.json` 의 `per_record` 가 이 퀘스트에서 처음으로 **개체별 전수 원자료**를
repo 밖에 남긴 산출물이다(70개체 × 기준 4종 × AUROC·SE·CI + 군 통계). Colab 에 repo 가
없으므로 감사는 **이 파일을 내려받아 로컬에서** `audit_anchor.py` 로 돌린다 —
노트북 안에서 다시 계산하지 않는다(노트북은 동결).
