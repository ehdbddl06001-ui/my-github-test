---
id: ailab-2026-0062
type: ailab
topic: Medical AI Lab
subtopic: ailab-2026-0046 실험 로그 — 두 축을 처음으로 동시에 통제했고, 형태만 살아남았다 (학습 0회) · svdb-recover-power [inter]
kind: log
week: 2
split: inter
metric: svdb_recover_stt_excess
value: 0.1837
passed: true
modality: ecg
dataset: svdb
notebook: notebooks/quest46_q7m_recover_power.ipynb
quest: ailab-2026-0046
step: svdb-recover-power
source: "MedKOS AI랩 실행 로그(ingest_run.py) · 실제 노트북 실행 결과"
confidence: high
date: 2026-08-03
tags: [ailab, run-log, week2, inter, svdb_recover_stt_excess]
related: [ailab-2026-0061, ailab-2026-0060, ailab-2026-0017]
---

## Overview

**두 축을 동시에 통제했다 — 이 퀘스트에서 처음이다. 그리고 그 조건에서 형태만 남았다.**

| 관문 | 결과 | 값 |
|---|---|---|
| **M1** `f1ₘ` < 0.505 | ✅ 지지 | **0.5000** [0.5000, 0.5000] · 짝잔여 **0.000000** |
| **M2 ★ 전제** `f2ₘ` < 0.56 | ✅ **지지** | **0.5078** [0.4711, 0.5483] · 짝잔여 **0.0049** |
| **M3a ★** (`P_late` 초과) − (`STT` 초과) | ⚠️ 미결 | **−0.0141** [−0.0892, +0.0476] · Bonf 3 |
| **M3b ★** (`STT` 초과) − (`f3` 초과) | ✅ 지지 | **+0.1642** [+0.0497, +0.3108] · Bonf 3 |
| **M4** 3축에서 `STT` | ⛔ **측정 불가** | 3축 가능 **6/59** — 표본이 없다 |
| **M5** `f3_glob` 반증 시험 | — | **절반 틀렸다**(§5) |

채점 59개체 · 유효 대역 0.02 · 정합 가능 **18** · 실행 479초. 학습 0회.

**Q7-K 는 두 축을 같은 폭 대역으로 갈라 어느 쪽도 못 죽였다**(`f1ₘ` 0.5526 · `f2ₘ`
0.5445). 이번엔 `pre` 를 **항상 정확 정합**으로 고정하고 **국소 기저선만 대역**으로
묶었다 — `pre` 를 고정하면 `f2 = 1 − pre/b` 는 **`b` 의 순증가 함수**라 둘은 정확히
같은 조작이다. 그 결과 `f1` 은 구성상 0.5000, `f2` 는 0.6729 → **0.5078**.

---

## 1. ★ 레거시 상수가 진짜 지렛대였다

| `MIN_CELL` | 정확 pre 만 | + 기저선 0.04 |
|---:|---|---|
| **3** (기존) | 25개체 · 남은 S **0.462** | **9개체** · 0.119 |
| 2 | 30개체 · 0.606 | 15개체 · 0.188 |
| **1** (이번) | **40개체** · 남은 S **0.812** | **20개체** · 0.464 |

조건부 일치도는 **칸마다 추정치를 만들지 않는다.** 이긴 쌍/전체 쌍을 누적할 뿐이라
**S 1 · N 1 칸도 유효한 쌍 1개**다. `≥3` 은 Q7-F 의 `matched_auc` 에서 물려받았고
정당화된 적이 없었다. **개체 25 → 40, 남은 S 46% → 81%.** 편의가 아니라 분산만 늘고,
그건 개체 부트스트랩이 잡는다.

**Q7-K 가 「두 축을 대역으로」 갈 수밖에 없다고 판단한 근거가 이 상수였다.** 상수를
고치니 원래 설계(정확 pre × 기저선 대역)가 그대로 작동한다 — 나는 **합성 픽스처만 보고
실데이터를 안 재본 채 설계를 버렸었다**(R24-b ④ 위반).

---

## 2. 두 축 통제 조건에서 무엇이 남았나

정확 pre × 기저선 0.02 (18개체 · 자기 라벨셔플 null 위 초과분 · 셔플 20회):

| 팔 | 실측 | null ± SE | **초과** |
|---|---:|---:|---:|
| `f1` | 0.5000 | 0.5000 ±0.0000 | **+0.0000** |
| `f2_16` (정합 대상) | 0.5078 | 0.4988 ±0.0078 | +0.0090 |
| **`f2_8`** | 0.5606 | 0.4974 ±0.0078 | **+0.0632** ⚠️ |
| `f3` (`post_rr`) | 0.5174 | 0.4979 ±0.0078 | +0.0195 |
| `f3_glob` | 0.4863 | 0.4984 ±0.0078 | −0.0120 |
| `f4` | 0.5295 | 0.4988 ±0.0082 | +0.0307 |
| `f5` | 0.4355 | 0.5011 ±0.0078 | −0.0656 |
| `lr_f1` | 0.3972 | 0.4216 ±0.0068 | −0.0244 |
| **`p_full`** | 0.6870 | 0.4701 ±0.0091 | **+0.2169** |
| **`stt`** | 0.6566 | 0.4730 ±0.0087 | **+0.1837** |
| **`p_late`** | 0.6475 | 0.4779 ±0.0091 | **+0.1696** |
| `stt_safe` | 0.6370 | 0.4750 ±0.0096 | +0.1620 |
| `p_early` | 0.5928 | 0.4697 ±0.0087 | +0.1231 |
| `lr_all` | 0.6564 | 0.4452 ±0.0096 | +0.2112 |

**조기성을 두 축으로 통제하면 리듬 특징은 전부 0 근처로 죽고 형태 창만 +0.16~0.22 로
남는다.** `lr_f1` 이 −0.0244 인 것이 계측기 검증이다 — `f1` 이 정확히 상수인 조건에서
로지스틱을 돌렸으니 자기 null 위 0 이어야 하고, 실제로 그렇다.

---

## 3. ★ M3a — P 파 고유의 신호는 없다 (세 번째 확인)

```
(P_late 초과) − (STT 초과) = −0.0141 [−0.0892, +0.0476]   → 미결
(참고) (P_full 초과) − (STT 초과) = +0.0332 [−0.0201, +0.0857]
(참고) (P_early 초과) − (STT 초과) = −0.0606 [−0.1483, +0.0203]
```

**P 가 사는 창이 P 와 무관한 ST/T 창을 이기지 못한다.** 사전등록한 판독 그대로다.
Q7-F(F2 `late < early` · F3 `STT > P_full`)와 Q7-K(L7 +0.0279 미결)에 이은 **세 번째
독립 확인**이고, 이번 것이 **가장 깨끗한 조건**에서 나왔다.

**상쇄 검증이 이 결론을 봉인 밖으로 꺼낼 자격을 준다** — `rho(개체별 (P_late−STT)
초과차, 개체별 `f2` 짝잔여) = **−0.007** [−0.540, +0.523]`. 잔여 오염이 두 창을 서로
다르게 부풀린다는 증거가 없다. ⚠️ 다만 **CI 가 매우 넓다(n=18)** — 「무상관을 확인했다」가
아니라 **「상쇄 가정이 반증되지 않았다」**가 정확한 표현이다.

**LOO 경고는 이 맥락에서 오히려 결론에 부합한다.** M3a 는 기준 −0.0141, LOO 범위
[−0.0247, +0.0044]이라 `#866` 을 빼면 부호가 뒤집힌다. 노트북이 「⚠️ 부호가 바뀐다」를
찍었는데, **효과가 0 인 양(量)은 당연히 부호가 흔들린다.** 경고 문구가 「0 근처」와
「불안정한 큰 효과」를 구별하지 못한다 → 노트북 수정 대상.
(M3b 는 [+0.1227, +0.1821]로 **부호가 안정**하다.)

---

## 4. `post_rr` 은 죽었다 — 실험1′ 의 독립 재현

```
f3      초과 +0.0195 [−0.0653, +0.0994]   → 0
f3_glob 초과 −0.0120 [−0.0987, +0.0689]   → 0
M3b (STT 초과) − (f3 초과) = +0.1642 [+0.0497, +0.3108] ✅
```

조기성을 두 축으로 통제하면 **보상성 휴지는 아무 것도 남기지 않는다.** 그리고 이건
`ailab-2026-0017` / `ailab-2026-0015` 의 **실험1′ 과 완전히 독립인 재현**이다:

| | 데이터 | 방법 | 결과 |
|---|---|---|---|
| 실험1′ | **Icentia11k 580환자** | 대규모 검정력 · `B3R − B3` | **+0.0049** [−0.0254, +0.0321] → 기각(확정) |
| Q7-M | **SVDB 18개체** | 정합 설계 · `f3` 초과 | **+0.0195** [−0.0653, +0.0994] → 0 |

**다른 데이터셋 · 다른 방법 · 같은 답.** 이 프로젝트에서 몇 안 되는 독립 재현이다.
「보상성 휴지는 RR 의 재조합이지 새 정보가 아니다」를 **확정**으로 올린다.

---

## 5. ★ M5 — 내 반증 시험이 나를 절반 정정했다

사전등록: 「`f3` 붕괴가 국소 기저선 때문이면 분모를 전역 중앙으로 바꾼 `f3_glob` 은
대역에 **반응하지 않아야** 한다.」 실측:

| 기저선 대역 | 정확pre | 0.10 | 0.06 | 0.04 | 0.02 |
|---|---:|---:|---:|---:|---:|
| `f3` | 0.5629 | 0.5148 | 0.5268 | 0.5043 | 0.5174 |
| `f3_glob` | **0.3850** | 0.4622 | 0.4849 | 0.4800 | 0.4863 |
| \|`f3` − 0.5\| | 0.0629 | 0.0148 | 0.0268 | 0.0043 | 0.0174 |
| \|`f3_glob` − 0.5\| | **0.1150** | 0.0378 | 0.0151 | 0.0200 | 0.0137 |

**`f3_glob` 도 반응했다.** 방향만 반대다 — `f3` 는 위에서, `f3_glob` 은 아래(0.3850,
강한 역방향)에서 **둘 다 0.5 로 수렴**한다.

→ **「`f3` 붕괴는 `f3` 의 분모가 국소 기저선이기 때문」이라는 좁은 해석은 지지되지
않는다.** 더 넓은 사실이 맞다: **국소 기저선을 맞추면 `post_rr` 기반 특징이 부호에
상관없이 전부 0.5 로 간다.** `post_rr` 이 담던 정보는 애초에 「이 박동이 얼마나 이른가」의
다른 표현이었다.

반증 시험을 사전등록해뒀기 때문에 **결과를 보고 해석을 고르지 않을 수 있었다.**

---

## 6. ⚠️ 내가 놓친 것 셋

### ① `f2_8` 이 살아남았다 — 「상대 조기성을 통제했다」는 과한 말이다

`LB_K = 16` 으로 정합했더니 `f2_16` 은 죽었는데(+0.0090) **`f2_8` 은 +0.0632 로 살아
있다.** 국소 기저선의 **창 길이가 다르면 다른 변수**다.

→ **정확한 진술은 「`f2_16` 을 통제했다」이지 「상대 조기성을 통제했다」가 아니다.**
개념을 통제하려면 여러 스케일을 동시에 맞춰야 하는데 표본이 안 된다 → **R28 ①**.
그래서 M3a·M3b(**차이**)가 여전히 M2 의 수준보다 신뢰할 만한 지표다.

### ② 고정 코호트를 안 썼다

대역 곡선에서 `STT` 가 0.5881(정확pre) → 0.6321 → 0.6628 → 0.6532 → **0.6566** 으로
**올라간다.** 그런데 개체가 40 → 27 → 24 → 20 → 18 로 바뀐다. **통제 효과와 코호트
효과가 섞여 있다** — Q7-I‴ 【K-E】 에서는 고정 코호트를 썼는데 여기서 빠뜨렸다.
곡선의 상승을 「통제할수록 형태가 강해진다」로 읽으면 안 된다.

### ③ M4 는 표본 문제 이전에 **인과적으로 잘못된 질문**이다

3축 정합은 6/59 로 측정 불가였는데, 사실 **표본이 있었어도 해석이 안 됐다.**
`post_rr` 은 **현재 박동보다 뒤에 오는 간격**이다. 현재 박동의 재분극(ST/T)이
미래 간격의 원인일 수는 있어도 그 반대는 아니다. 즉 `post_rr` 은 교란이 아니라
**S 이기 때문에 생기는 결과**(하류 변수)다. **결과를 통제하면 신호가 지워진다** —
`STT` 가 사라져도 「형태가 가짜였다」가 아니라 「매개변수를 막았다」일 뿐이다.
→ **M4 를 rate hysteresis 의 시험으로 쓰지 않는다. 설계를 폐기한다** → **R28 ②**.

**rate hysteresis 는 이미 다른 경로로 반증에 가깝다**: QT 는 **직전** RR 의 함수인데
정확 pre 정합이 그걸 맞춘다. 그리고 이력 의존성은 국소 기저선이 대리하는데 그것도
0.02 로 맞췄다. 그 조건에서 `STT` 가 **+0.1837** 로 남는다.

---

## 7. 선택 편향 — 결론의 적용 범위

| 층 | 전체 | 2축 | 3축 |
|---|---:|---:|---:|
| 고립 S | 42 | **14** | 5 |
| 혼합 | 12 | **3** | 0 |
| 런 우세 | 5 | **1** | 1 |

RR 분리도 정합 **0.2676** vs 제외 0.3348 · 유병률 정합 **0.1048** vs 제외 0.0294.
Q7-K(19/59)와 사실상 같은 편향이다 — `MIN_CELL=1` 이 **정확 pre 단독**에서는 40개체를
회복했지만, 기저선 대역 0.02 를 얹으면 18 로 돌아온다.

→ **이 결론은 「고립 S 가 많고 유병률이 높으며 RR 분리가 덜 뚜렷한 환자」에 한정된다.**
혼합 3 · 런 1 로는 그 층에 대해 아무 말도 못 한다.

**층화 vs 매크로**: 형태 팔에서 −0.05 수준으로 벌어진다(`stt` 0.6032 vs 0.6566 ·
`p_late` 0.6007 vs 0.6475 · `lr_all` 0.6073 vs 0.6564). 순수 특징은 ±0.008 이내다.
**큰 개체가 형태 창의 방향을 끌고 있다**는 신호이고, 사전 선언대로 매크로를 주지표로
쓰되 이 간극을 함께 적는다.

---

## 8. 배운 것

1. ★ **정당화 없는 상수는 결론을 바꾼다.** `MIN_CELL = 3` 하나가 개체 25 → 40,
   남은 S 46% → 81% 를 좌우했고, 그것 때문에 Q7-K 가 설계를 바꿔 실패했다.
   **물려받은 상수는 실험마다 감사한다** → **R28 ③**.
2. ★ **「통제했다」는 그 특징의 특정 파라미터화에만 성립한다**(`f2_16` ✅ · `f2_8` ❌)
   → **R28 ①**.
3. ★ **교란만 정합한다. 하류 변수를 정합하면 신호를 지운다**(`post_rr`) → **R28 ②**.
4. **사전등록한 반증 시험이 값을 했다.** M5 가 내 해석을 절반 정정했고, 결과를 보고
   해석을 고르는 걸 막았다.
5. **0 근처 값의 LOO 부호 반전은 경고가 아니다.** 경고 문구가 「0 이라서 흔들림」과
   「큰 효과인데 불안정」을 구별해야 한다.

---

## 9. 확정된 것 / 다음

**확정**
- **`post_rr`(보상성 휴지)의 독립 기여는 없다** — 실험1′ 과 독립 재현
- **P 파 고유의 신호는 없다** — Q7-F · Q7-K · Q7-M 세 번째 확인
- **형태 신호는 조기성을 두 축으로 통제해도 남는다** — `STT` +0.1837 ·
  `STT_SAFE` +0.1620 · `P_full` +0.2169 (창 폭 85 인 것들끼리는 서로 구별 안 됨)

**다음 — Q7-N `cross-patient-template`(= Q7-G 재개)**

Q7-G 는 「형태 축이 진짜인지 모른다」는 이유로 보류돼 있었다. **그 조건이 이제
충족됐다.** 그리고 지금까지의 형태 값은 전부 **개체 내부 템플릿**이라 **라벨을 쓰는
상한**이다 — 실제로 쓰려면 다른 환자로 만든 템플릿이어야 한다.

- **N1 ★** 개체를 뺀 나머지 개체들의 N/S 중앙으로 만든 **교차환자 템플릿**을
  M2 조건(정확 pre × 기저선 0.02)에서 채점 — 초과분 > 0 인가
- **N2** 개체 내부 템플릿(상한) 대비 **회복률 %**
- **N3** 진폭 정규화 방식을 **사전등록**하고, 방식에 따라 결론이 갈리면 그 사실을
  결과로 쓴다(R18)
- **N4** 창 폭을 **맞춰서** P vs ST/T 재확인(R27 ③) · `f2_8` 도 정합 축에 추가한
  민감도(다중 스케일 · R28 ①)
- **N5** **고정 코호트** 곡선(§6 ②) · LOO 경고 문구 수정 · 층화-매크로 간극 병기
- **M4 는 폐기**(§6 ③ · R28 ②)
