---
id: ailab-2026-0068
type: ailab
topic: Medical AI Lab
subtopic: 퀘스트46 인수인계 ② — Q7 형태 갈래 종결 · 새 세션은 이 카드로 Q3(무라벨 EM 사전확률 보정) 설계를 시작한다
kind: mentor
week: 2
modality: ecg
dataset: svdb
quest: ailab-2026-0046
source: "MedKOS 세션 인수인계 노트 — 기존 카드·노트북·규칙집의 요약이며 새 실측은 없다. 숫자는 전부 공식 실행 로그에서 옮겼다"
confidence: high
date: 2026-08-04
tags: [ailab, handoff, quest46, week2, calibration]
related: [ailab-2026-0046, ailab-2026-0064, ailab-2026-0053, ailab-2026-0067]
---

## 0. 이 카드의 용도

세션(대화창)이 길어지면 컨텍스트가 요약되면서 세부가 흐려진다. 이 프로젝트는
CLAUDE.md 의 「임시 컨테이너 대응」 원칙대로 **상태를 대화가 아니라 `content/` 에** 둔다.
이 카드는 **`ailab-2026-0064` 의 후속 인수인계 노트**다. 0064 는 Q7-A~Q7-N 을 덮고,
이 카드는 **Q7-T ~ Q7-AA(형태 갈래 종결)와 그다음 갈래인 Q3** 를 덮는다.

> **새 세션은 이 순서로 읽으면 그대로 이어서 일할 수 있다.**
> ① 이 카드 → ② `content/ailab/quests/ailab-2026-0046.md` 의 `next_goal`
> → ③ `pipelines/SCORING_RULES.md` 의 **R35~R41** → ④ 필요하면 개별 로그 카드.

⚠️ 이 카드는 **요약이지 원본이 아니다.** 숫자를 인용해야 하면 원본(노트북 출력·로그 카드)을 본다.

### 새 창에 붙여 넣을 프롬프트

```
퀘스트46(content/ailab/quests/ailab-2026-0046.md)의 다음 갈래인 Q3
(prior-em-calibration · 무라벨 EM 사전확률 보정)를 설계한다.
먼저 content/ailab/mentor/ailab-2026-0068.md 를 읽고,
이어서 퀘스트 카드의 next_goal 과 pipelines/SCORING_RULES.md 의 R35~R41 을 읽어라.
Q7 형태 갈래는 종결됐으니 되살리지 말고, Q3 노트북을 사전등록 관문 형태로 설계해서
빌더 + 픽스처 + 스모크런까지 만들어라. 내가 Colab 에서 돌리고 결과를 준다.
```

---

## 1. 퀘스트 46 이 묻는 것 (안 바뀜)

> **S(상심실 조기박동, PAC)는 「환자 자기 baseline 대비 이르다」는 맥락 상대량으로 정의되는데,
> 그걸 어떻게 재고 보정해야 환자 간(inter-patient)에서 안 무너지나.**

층이 넷이고 서로 다른 도구가 필요하다.

| 층 | 무엇 | 상태 |
|---|---|---|
| ① 표현 | 환자상대 특징(`_medref` · RR innovation) | 상당히 풀림(within S 0.7049 → 0.9166) |
| ② **점수 눈금** | 기록마다 다른 유병률 → 점수 비교 불가 | ★ **여기가 다음 차례 = Q3** |
| ③ 채점 | 환자 매크로 · R11 / R11-b | ✅ 규약 확립 |
| ④ 코호트 | 지배 지분 · 채점 가능 환자 수 | ✅ SVDB 로 해결(지배 14.9% · GMIN10 에서 66개) |

**층② 진단은 이미 끝났다**: 기록 내 순위정규화가 전역 하락의 **95.4%** 를 회복한다
→ 눈금이 주범. **처방이 아직 없다.** 순위정규화 자체는 처방으로 못 쓴다 —
정규화하면 두 팔 다 절대 성능이 폭락한다(**PR-AUC 0.5138 → 0.0851**).
「이 기록은 S 가 많다」도 진짜 정보이고 홀터 판독에서 버리면 안 된다.

---

## 2. Q7 **형태 갈래 종결** — 이 세션이 닫은 것

### 2.1 최종 문장 (그대로 인용해도 되는 결론)

> **2리드 홀터 SVEB 검출에서, RR 기반 리듬 특징을 통제한 뒤 P 파 형태는
> 환자 안에서는 정보를 담지만**(부담 상위군 매칭 AUROC **0.5608** [0.5170, 0.6097]
> vs 라벨치환 null **0.4991**, 초과 **+0.0617**)**, 교차환자 예측 효용으로는 전환되지 않는다**
> (같은 군 ΔAUPRC **−0.0007** [−0.0053, +0.0030] · 리듬 기저 AUPRC 0.416 대비 상한 **+0.7%**).
> **이 값은 측정 오차 보정으로 부풀릴 수 없다** — 검출기 잡음을 절반으로 줄여도
> (λ 0.26 → 0.49) 판별력은 변하지 않았다(Q7-Z).

핵심 표현: **기전/효용 해리(dissociation)**. 기전은 실재하고, 부담이 높은 환자에게
집중되며, 그럼에도 **환자를 건너가지 않는다** → P 파 형태 정보는 **환자 특이적**이다.
이게 오래된 Q7-R 의 R1 vs R5 모순을 해소한다.

### 2.2 마지막 런 Q7-AA (공식 실행 `20260804T1104`) 실측

```
AA0 ✅  S3 전체 0.5421 [0.4925, 0.5933] vs Q7-Z 0.5421 · |Δ| 0.000045   (재현 확인)
        부담 분포(판정 56개) 중앙 0.0447 · 범위 0.0070~0.5764
        고부담 23개(중앙 0.1205 · 쌍 1,302) · 저부담 11개(0.0195 · 369)
AA3 ✅  영점  전체 0.4989 [0.4952,0.5025] · 고 0.4991 [0.4969,0.5014] · 저 0.4985 [0.4878,0.5089]
AA1 ✅  고부담 0.5608 [0.5170, 0.6097] · null 0.4991 · 초과 +0.0617 · n=23 · MDE 0.0463
AA2 ⚠️  저부담 0.5031 [0.3893, 0.6206] · null 0.4985 · 초과 +0.0046 · n=11 · MDE 0.1157
        부분군 차(고−저) +0.0577 [−0.0670, +0.1823]      ← 차 자체는 미결
AA4 ✅  부담 ρ +0.0946 vs 영점 −0.0339 [−0.0822,+0.0135] · 쌍수 ρ +0.2898 vs [−0.0735,+0.0025]
AA5 ⚠️  ★결정 숫자★ 고부담 W1 −0.0007 [−0.0053, +0.0030] n=28
        (성분 palign −0.0004 · shuf5 +0.0004 · noise5 −0.0001)
        저부담 W1 −0.0095 [−0.0301, +0.0065] n=28 · 전체(Q7-S″) W1 +0.0048 [−0.0030, +0.0141]
AA6     S3 전체 +0.0416/0.0504 → 50/101 · S3 고부담 +0.0617/0.0463 → 13/26
        W1 고부담 −0.0007/0.0042 → 917/1871  (효과 ≈ 0 이라 해석 불가)
```

★ **AA5 의 MDE 0.0042 는 전체 코호트의 0.0085 보다 좁다.** 즉 이건 검정력 부족이
아니라 **정밀한 0** 이다. 「표본을 더 모으면 될 것 같다」로 되살리지 마라.

### 2.3 이 갈래에서 **무너진 주장들** (되살리면 안 되는 것)

| 폐기·철회된 것 | 왜 |
|---|---|
| 「**자(검출기)가 병목이다**」 | **Q7-Z 가 직접 기각**. λ 0.2586 → 0.4898 로 거의 2배인데 S3 초과는 +0.0421 → +0.0407, 차 −0.0015 [−0.0217,+0.0228] 이 예측 차 +0.0409·+0.0354 를 **둘 다 배제** |
| X6 의 탈감쇠 상한(+0.0488·+0.0545) | 위 기각으로 **전부 무효** |
| 「λ 0.57 이면 필요표본 31/63」 | 폐기 |
| **Q7-Y**(이동 불변 매치드필터) | 반증된 전제 위 설계라 **짓지 않았다** |
| 「부담 상위군을 표적 모집단으로」 | AA5 가 **정밀한 0** → **표적 모집단으로서의 burden 은 닫혔다** |

⚠️ **단, `burden` 을 「특징」으로 쓰는 Q4 는 안 닫혔다.** AA4 에서 부담↔AUROC 연관은
영점을 넘었다(ρ +0.2898). **모집단 절단**과 **특징 투입**은 다른 것이다.

### 2.4 완료·동결된 노트북 (전부 `notebooks/`)

| 노트북 | 픽스처 | 상태 |
|---|---|---|
| `quest46_q7t_p_anchored.ipynb` | `test_quest46_q7t.py` 20/20 | 완료·동결 |
| `quest46_q7u_public_delineator.ipynb` | 21/21 | 완료·동결 |
| `quest46_q7p0_svdb_pdelin.ipynb` | 13/13 | 완료 (**자산 생성 런**) |
| `quest46_q7s2_p_aligned.ipynb` | 15/15 | 완료·동결 |
| `quest46_q7s3_recompute.ipynb` | `test_quest46_q7s3.py` 37/37 | 완료 |
| `quest46_q7v_ruler_audit.ipynb` | 50/50 | 완료 |
| `quest46_q7x_diagnostics.ipynb` | 47/47 | 완료 |
| `quest46_q7z_direct_test.ipynb` | 55/55 | 완료 |
| `quest46_q7aa_burden_target.ipynb` | `test_quest46_q7aa.py` 36/36 | 완료 · **갈래 종결** |

---

## 3. 다음 갈래 — **Q3 `prior-em-calibration`**

### 3.1 사전등록된 질문과 합격 기준 (퀘스트 카드 실험 큐 원문)

> **Q3 · `prior-em-calibration`** — 방법 A(**무라벨 EM 사전확률 보정**)가 눈금을 고치나
> **합격 기준**: 전역 PR-AUC 회복 **≥ 50%** 이면서 매크로가 **안 떨어질 것**

방법 A 의 출처: **Saerens · Latinne · Decaestecker (2002)** — EM prior adjustment.
테스트 분포의 클래스 사전확률을 라벨 없이 EM 으로 추정해 로짓을 옮긴다(표준 기법).

### 3.2 ★★ 설계 전에 반드시 풀어야 하는 긴장 (이걸 모르면 Q3 은 자명한 0 을 잰다)

**레코드별 사전확률 보정은 그 레코드 안에서 상수 로짓 시프트다 → 레코드 내 순위를
보존한다 → 레코드별 AUROC·PR-AUC 가 정의상 정확히 불변이다.**

즉 **매크로 지표는 항등으로 통과**한다. 「매크로가 안 떨어질 것」이라는 합격 기준은
정보량 0 인 관문이 될 수 있다. 그래서 Q3 설계의 **첫 결정**은 이것이다:

1. **주 지표를 무엇으로 둘 것인가.** 보정이 움직일 수 있는 건 **전역(pooled) 지표**뿐이다.
   그런데 R11 은 전역을 **보조**로 격하했다. 이 모순을 **설계 문서에서 먼저 해소**하라 —
   가능한 정리: 「Q3 의 표적은 **환자 간 점수 비교 가능성**이고, 그건 정의상 pooled 지표에서만
   보인다. 따라서 Q3 한정으로 주 지표는 **전역 PR-AUC**, 매크로는 **항등 대조**(움직이면 버그)」.
2. **매크로 비열등 관문을 「항등 검증」으로 재정의**하라 — 매크로가 **정확히** 안 움직이는지
   확인하는 관문(R34: 음성/항등 대조는 **구성으로 보장**). 움직이면 보정이 비단조라는 뜻이다.
3. 비단조 보정(클리핑·기록별 임계값 등)을 팔에 넣는다면 그때는 매크로가 **진짜** 판정 대상이 된다.
   그 경우 어느 팔이 어느 관문에 걸리는지 **팔별로 사전등록**하라.

### 3.3 설계에 반드시 넣을 것 (Q7 에서 비싸게 배운 것들)

| # | 요구 | 근거 규칙 |
|---|---|---|
| A | ★ **오라클 사전확률 팔**을 상한으로 같이 재라 — 진짜 유병률로 보정했을 때의 값. 이게 없으면 「방법이 나쁜 건지 **추정**이 나쁜 건지」 못 가른다 | R35 ①(자를 먼저 세워라) |
| B | ★ **항등 대조** — 사전확률을 학습셋 사전확률 그대로 두면 보정이 **정확히 항등**이어야 한다. 구성으로 통과시켜 증명 | R34 · R37 |
| C | ★ **영점(null)을 측정하라** — 문턱 0.5 나 「0 이면 0」을 가정하지 마라. 학습이 끼면 영분포는 0.5 가 아니다 | R26 · R38 ② |
| D | **MDE 를 효과크기 단위로 병기**하고, 미결이면 「미결」이라고 써라. 미결 ≠ 등가 | R29 · R30 · R33 ① |
| E | **필요 표본은 프레임(우월/등가)을 밝혀서** 계산하라. 그리고 **효과가 0 근처면 필요표본은 해석하지 마라** | R37 ① · **R41 ②** |
| F | 하이퍼(EM 반복수·수렴 문턱·평활)를 **보고할 지표로 고르면 누출**. LORO 또는 DEV 반쪽에서 고정 | R22 · R36 ② |
| G | **결론 검산표를 코드에 박아라** — 요약 셀이 관문 판정과 모순되지 않게 | R38 ⑦ · R39 ⑤ |
| H | 지배 지분·채점 제외 환자·`GMIN_S` 를 **명시**하고, 전역 단독 인용 금지 조건을 지켜라 | R11 · R11-b |
| I | 예측 모형을 세웠으면 **그 모형을 직접 검정하는 관문**을 먼저 넣어라(Q7-Z 가 반나절을 아꼈다) | R40 ① |

### 3.4 이미 손에 있는 출발 숫자

| 사실 | 값 | 출처 |
|---|---|---|
| SVDB 전수 재채점 환자 매크로 AUROC | **0.8842** [0.8449, 0.9175] · 72개체 | Q7-B′ (`ailab-2026-0053`) |
| 리듬-only 기저 AUPRC(교차환자) | **0.416** | Q7-S″ / Q7-AA 기저 |
| 눈금 붕괴 기여 | 전역 하락의 **95.4%** | 실험22-A |
| 순위정규화의 대가 | PR-AUC 0.5138 → **0.0851** | 07-17 SVDB 증강 실험 |
| SVDB 지배 지분 / 채점 가능 | **14.9%** / GMIN10 에서 **66개** | Q7-A (`ailab-2026-0049`) |
| 부담↔성능 연관(영점 초과) | ρ **+0.2898** | Q7-AA AA4 → **Q4 의 씨앗** |

### 3.5 Q3 다음은 Q4

**Q4 `burden-feature`** — 방법 B(기록 burden 을 **별도 특징**으로)가 A 보다 나은가.
합격 기준(사전등록): Q3 대비 전역 PR-AUC **> 0** · 매크로 **비열등**.
Q7-AA 가 닫은 건 **표적 모집단으로서의 burden** 이지 **특징으로서의 burden** 이 아니다(§2.3).

---

## 4. 작업 규약 (이 프로젝트의 실험 루프)

```
① 설계        사전등록 관문(무엇이 ✅/⚠️/❌ 인지)을 먼저 글로 확정
② 빌더        스크래치패드에 build_<런>.py  →  notebooks/quest46_<런>.ipynb 생성
③ 픽스처      pipelines/test_quest46_<런>.py — 정적(코드 검사) + 동적(합성 데이터)
④ 스모크런    축소 조건으로 전 셀 실행. 설계 결함은 여기서 잡는다(실제로 여러 번 잡았다)
⑤ 사용자 실행  Colab 에서 공식 실행 → 로그·그림·result.json 을 대화로 준다
⑥ 해석        관문대로 읽는다. 관문이 발화했으면 사후에 기준을 바꾸지 않는다
⑦ ingest      python pipelines/ingest_run.py --results result.json --notebook notebooks/<파일>.ipynb
              → content/ailab/logs/ 에 kind: log 카드(수치는 실측 · LLM 이 안 지어냄)
⑧ 규칙집      새로 배운 것을 SCORING_RULES.md 에 R번호로 누적
```

**커밋 전 필수 순서**(CLAUDE.md): `git fetch origin main && git merge origin/main`
→ `python pipelines/indexer.py --check` → `python pipelines/indexer.py` → `docs/` 번들 재생성
→ 새 `.md` 와 번들을 **함께** 커밋.

**개발 브랜치**: `claude/ai-model-ecg-diagnosis-6v8hof`.
PR 은 **명시적으로 요청받았을 때만** 만든다.

---

## 5. 남은 부채 (새 세션이 알아야 할 것)

1. ★ **ingest 미실행 9건** — `but-pdb-validate` · `public-delineator` · `svdb-pdelin` ·
   `p-aligned-personalize` · `recompute-frames` · `ruler-audit` · `lambda-diagnostics` ·
   `deattenuation-test` · `burden-target`. 숫자는 노트북 출력과 퀘스트 카드에 남아 있지만
   **로그 카드(`kind: log`)가 없다** → `deepen.py` 가 이 런들을 못 읽는다.
   Colab `result.json` 이 있으면 그때 돌린다(없으면 못 만든다 — LLM 이 지어내지 않는다).
2. `claude/ai-model-ecg-diagnosis-6v8hof` → `main` 머지.
3. Q7-E′(조건부 앵커)·Q5(기록별 경보율)·Q6(소급 재채점)은 여전히 큐에 남아 있다.

---

## My notes

- 이 세션에서 가장 값이 컸던 결정은 **Q7-Y 를 짓기 전에 Q7-Z 를 돌린 것**이다.
  Q7-Y 는 「자를 개선하면 효용이 오른다」는 전제 위에 있었고, Q7-Z 가 그 전제를 죽였다.
  **모형이 다음 런의 설계 전제라면, 그 모형을 먼저 직접 검정한다**(R40 ①).
- 두 번째로 값이 컸던 건 **버그 있는 코드의 출력에 재현 기준을 앵커하지 않은 것**이다.
  Q7-Z 의 Z0 이 corr 0.7122 로 막혔을 때, 그 0.7122 자체가 이미 폐기한 코드의 산물이었다.
  기준을 **구성적 항등**으로 바꾸자 corr 1.000000 이 나왔다(R39 보론).
- 세 번째는 **λ 는 신뢰도이지 타당도가 아니다**(R40 ①). 통계량이 자기 자신과 일치하는 정도와
  라벨과 관계있는 정도는 다른 것이고, 나는 이걸 한 갈래 내내 혼동했다.
